package SeleniumTraining.Fundamentals;

import org.openqa.selenium.SearchContext;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chromium.ChromiumDriver;
import org.openqa.selenium.remote.RemoteWebDriver;

public class TopCastingOptions {

    public static void main(String[] args) {


        // topcasting options
//        0. not top casting, it will work only for chrome. local only
//        ChromeDriver driver = new ChromeDriver();
//        1. Valid and recommended -- Local
//        WebDriver driver = new ChromeDriver();
//        2. Valid but not recommended.
//        SearchContext driver = new ChromeDriver();
//        3. Valid and recommended -- Local
//        RemoteWebDriver driver = new ChromeDriver();
//        4. Valid and recommended, Only to run test cases on remote machine - grid, Server, docker, cloude, machime, LT, SL...etc
//        WebDriver driver = new RemoteWebDriver(remote address , capability );
//        5. valid but not recomanded
//        SearchContext driver = new RemoteWebDriver();
//        6. Valid but not recommended We can use only if use need to work only with Chrome & edge then only.
//        ChromiumDriver driver = new ChromeDriver();




    }

}
